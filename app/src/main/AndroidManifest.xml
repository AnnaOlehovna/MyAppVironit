<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vironit.poddubnaya.myappvironit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".mvp.presentation.view.implementation.activity.SplashActivity"
            android:theme="@style/AppTheme.Splash"
            android:noHistory="true"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity android:name=".mvp.presentation.view.implementation.activity.MainActivity"/>

        <activity android:name=".mvp.presentation.view.implementation.activity.base.DummyActivity"
            android:theme="@style/AppTheme.TransparentActivity"
            android:noHistory="true"/>


    </application>


</manifest>
